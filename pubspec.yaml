name: study_project
description: A Flutter projects where I can gather differents study projects with nothing in particular except the code.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  easy_image_viewer: ^1.2.0
  pinch_zoom: ^2.0.0
  material_color_utilities: ^0.5.0
  flutter_lints: ^3.0.1
  path: ^1.9.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - lib/assets/
    # - lib/assets/images/
    - lib/assets/mobileBankApp/
  # fonts:
  #   - family: BigelowRules
  #     fonts:
  #     - asset: lib/assets/fonts/BigelowRules-Regular.ttf
  
